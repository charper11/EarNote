import { Button } from '@mui/material';

const Home = () => {
    return (
        <div>
          <span>Ear Note</span>
          <Button href="/unit">Unit</Button>
        </div>
    );
};

export default Home;